#!/bin/sh
export JAVA_OPTS=<%= node["tomcat8"]["java_options"] %>
case "$1" in
start)
  <%= node["tomcat8"]["tomcat_install_loc"] %>/bin/startup.sh
;;
stop)
  <%= node["tomcat8"]["tomcat_install_loc"] %>/bin/shutdown.sh
;;
*)
  echo "Syntax: $0 <start|stop>"
esac
